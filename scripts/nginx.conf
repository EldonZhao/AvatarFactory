worker_processes 1;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile on;
    keepalive_timeout 65;

    # Upstream servers
    upstream api {
        server 127.0.0.1:8000;
    }

    upstream dashboard {
        server 127.0.0.1:8501;
    }

    upstream chronicle {
        server 127.0.0.1:4321;
    }

    server {
        listen 80;
        server_name _;

        # Chronicle SSR - proxy to Node.js server
        location /chronicle/ {
            proxy_pass http://chronicle/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Chronicle static assets (higher priority for caching)
        location /chronicle/assets/ {
            proxy_pass http://chronicle/assets/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Redirect /chronicle to /chronicle/
        location = /chronicle {
            return 301 /chronicle/;
        }

        # API endpoints - route to FastAPI
        location /health {
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /docs {
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /openapi.json {
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /redoc {
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /api/ {
            proxy_pass http://api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # API routes without /api prefix (lowercase paths only)
        # Streamlit Dashboard pages use capitalized names like /Content, /Personas
        # which will fall through to the default location (dashboard)
        location ~ "^/(personas|content|tasks|connectors|topology|chat|generate|discover|publish|schedule|scheduler)(/.*)?$" {
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Explicitly route Dashboard pages (capitalized) to Streamlit
        location ~ "^/(Content|Personas|Scheduler|Topology|Connectors|Discovery|Topics|Chat)(/.*)?$" {
            proxy_pass http://dashboard;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
        }

        # Dashboard (Streamlit) - everything else
        location / {
            proxy_pass http://dashboard;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
        }

        # Streamlit specific paths
        location /_stcore {
            proxy_pass http://dashboard;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
        }

        location /static {
            proxy_pass http://dashboard;
            proxy_set_header Host $host;
        }
    }
}
